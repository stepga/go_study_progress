package main

import (
	"fmt"
)

func Sqrt(x float64) (float64, error) {
	// TODO copy sqrt from flowcontrol 08 and modify it to return an error value
	return 0, nil
}

func main() {
	fmt.Println(Sqrt(2))
	fmt.Println(Sqrt(-2))
}
